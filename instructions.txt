// 1. Create server.js file; initialize npm and install dependencies: express, dotenv, cors, mongodb, ejs, nodemon (--save-dev). Add nodemon dev script to package.json

// 2. Require dependencies

// 3. Declare variables

// 4. Connect to MongoDB - add connection string to .env file

// 5. Create Port

// Test Mongo and Port Connection

// 6. Create gitignore file, include node_modules and .env

// 7. Git init, set up repo and push

// 8. Set middleware (prior to any CRUD functions)

// 9. Create public and views folders; add main.js and style.css to public, and index,ejs to views

// 10. Add content to main.js, style.css, index.ejs

// 11. Create heroku repo:

    // heroku login
    // heroku create simple-rap-api
    // git add .
    // git commit -m "changes"
    // git push heroku main
    // --Test Heroku link--



// Notes: you can set up multiple Mongo connections if your app uses multiple databases.
// To manually start server, enter node server.js
// To start server via nodemon, enter npm run dev